name: CI/CD + OWASP Security Scan

on:
  push:
    branches: [ main ]

jobs:
  build-scan-deploy:
    runs-on: ubuntu-latest

    steps:
    # -----------------------------
    # 1. Checkout Code
    # -----------------------------
    - name: Checkout Repository
      uses: actions/checkout@v4

    # -----------------------------
    # 2. Setup Node.js 20
    # -----------------------------
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    # -----------------------------
    # 3. Install Dependencies
    # -----------------------------
    - name: Install Dependencies
      run: npm install

    # 🔥 IMPORTANT: Generate lock file (fix OWASP warnings)
    - name: Generate package-lock.json
      run: npm install --package-lock-only

    # -----------------------------
    # 4. Build Application
    # -----------------------------
    - name: Build Application
      run: npm run build

    # -----------------------------
    # 5. OWASP Dependency Check
    # -----------------------------
    - name: Run OWASP Dependency Check
      uses: dependency-check/Dependency-Check_Action@main
      with:
        project: "js-webapp"
        path: "."
        format: "JSON"
        out: "reports"

    # -----------------------------
    # 🔥 FIX PERMISSIONS (your error fix)
    # -----------------------------
    - name: Fix permissions
      run: |
        mkdir -p reports
        chmod -R 777 reports

    # -----------------------------
    # 6. Generate Custom Report
    # -----------------------------
    - name: Generate Custom Vulnerability Report
      run: python3 scripts/owasp_report.py

    # -----------------------------
    # 7. Fail on Critical Issues
    # -----------------------------
    - name: Fail on Critical Vulnerabilities
      run: |
        if grep -q "Critical" reports/final-report.txt; then
          echo "❌ Critical vulnerabilities found!"
          exit 1
        fi

    # -----------------------------
    # 8. Upload Report Artifact
    # -----------------------------
    - name: Upload Security Report
      uses: actions/upload-artifact@v4
      with:
        name: vulnerability-report
        path: reports/final-report.txt

    # -----------------------------
    # 9. Deploy to EC2
    # -----------------------------
    - name: Deploy to EC2
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.EC2_HOST }}
        username: devops
        key: ${{ secrets.EC2_SSH_KEY }}
        port: 2222
        script: |
          cd /home/devops/js-webapp
          git pull origin main

          export NVM_DIR="$HOME/.nvm"
          source $NVM_DIR/nvm.sh

          npm install
          npm run build

          pm2 restart nextjs-app || pm2 start npm --name "nextjs-app" -- start
